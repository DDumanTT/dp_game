{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "serve": "npm run build:client && npm run serve:ngrok",
    "serve:ngrok": "concurrently --kill-others --raw \"cd ../ && ngrok http 3000\" \"cross-env NODE_ENV=production node app.js\"",
    "build:client": "cd ../client && npm run build && ncp ./dist ../server/public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.4.0",
    "cross-env": "^7.0.3",
    "express": "^4.18.1",
    "socket.io": "^4.5.2"
  },
  "devDependencies": {
    "ncp": "^2.0.0",
    "nodemon": "^2.0.20"
  }
}
