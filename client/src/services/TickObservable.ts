import IAutoService from "../core/interfaces/IAutoService";
import IGameManager from "../core/interfaces/IGameManager";

export default class TickObservable implements IAutoService {
  execute(gameManager: IGameManager) {

  }
}
