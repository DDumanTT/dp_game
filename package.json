{
  "name": "dp_game",
  "scripts": {
    "build": "npm run build -ws",
    "start:server": "ts-node -r tsconfig-paths/register server/dist/src/index.js",
    "start:client": "npx http-server client/dist/ --cors -a 0.0.0.0 -p $PORT"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.0",
    "typescript": "^4.8.3"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ]
}
